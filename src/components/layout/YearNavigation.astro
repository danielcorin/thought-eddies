---
import YearPill from "@components/YearPill.astro";

interface Props {
  years: number[];
  baseUrl: string;
}

const { years, baseUrl } = Astro.props;
---

<nav class="year-navigation soft-divider-before">
  <h2>Browse by year</h2>
  <div class="year-links">
    {
      years.map((year) => (
        <YearPill year={year} href={`${baseUrl}/${year}`} />
      ))
    }
  </div>
</nav>

<style>
  .year-navigation {
    margin-top: var(--spacing-xl);
    padding-top: var(--spacing-lg);
  }

  .year-navigation h2 {
    font-size: var(--text-xl);
    margin-bottom: var(--spacing-md);
  }

  .year-links {
    display: flex;
    gap: var(--spacing-md);
    flex-wrap: wrap;
  }

</style>
